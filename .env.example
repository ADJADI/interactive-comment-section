# Copier ce fichier en ".env" à la racine avant de lancer "docker compose -f docker/docker-compose.yml up --build"

# Nom de la base MongoDB
MONGO_DB=interactive_comments

# Origine autorisée par le CORS du serveur GraphQL (l'URL publique du client)
CORS_ORIGIN=http://localhost:8080
