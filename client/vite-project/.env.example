# URL du serveur GraphQL utilisée par le client en développement local (npm run dev)
# En production, cette valeur est injectée au moment du build Docker (voir docker-compose.yml)
VITE_GRAPHQL_URI=http://localhost:4000/graphql
